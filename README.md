# SecureLink-Core


## Development Prerequisites

In order to run the build, you will need the following installed:

* **GNU MAKE** and **GCC**.
* **Python 3.11** (Required for gPRC compatibility).


### Setup & Build
In the parent directory run:
```bash
make
```
